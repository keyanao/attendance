import {
  getDoc,
  doc,
  getDocs,
  collection,
  query,
  where,
  orderBy,
  limit,
  updateDoc,
} from "firebase/firestore";
import { db } from "../FirebaseConfig";

export const timePlus = async (uid, minute) => {
 
};
